${SHARP} ---
${SHARP} Created on $today.date ( Time $today.time )
${SHARP} Generated by $generator.name ( version $generator.version )
${SHARP} ---
#if ( $databases.hasDatabase( $model.databaseId ) )
#set($database = $databases.getDatabase( $model.databaseId )) 
${SHARP} Database configuration  
redis.hostname=$database.hostname
redis.port=$database.port
redis.username=$database.user 
redis.password=$database.password
#else

${SHARP}
${SHARP} REDIS default configuration
${SHARP} Redis hostname 
redis.hostname=localhost
${SHARP} Redis default port
redis.port=6379
${SHARP} User name
redis.username= 
${SHARP} User password
jdbc.password=
#end